import { ResolvedStatusPipe } from './resolved-status.pipe';

describe('ResolvedStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ResolvedStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
