import { FeedbackComplaintTypePipe } from './feedback-complaint-type.pipe';

describe('FeedbackComplaintTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FeedbackComplaintTypePipe();
    expect(pipe).toBeTruthy();
  });
});
